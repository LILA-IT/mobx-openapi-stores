nodeLinker: node-modules

# Include darwin (local) and linux/glibc (CI) so native optional deps such as
# fallow's platform binaries land in the lockfile for both.
supportedArchitectures:
  cpu:
    - x64
    - arm64
  libc:
    - glibc
  os:
    - darwin
    - linux
